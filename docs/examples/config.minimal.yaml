# Minimal config example for docs/config.md and docs/adapters.md.
# Replace "." with the directory that contains your evaluation inputs.
version: 1

data:
  input_dir: "."

adapter:
  type: "dummy"

runner:
  warmup_iterations: 3
  continue_on_error: false
